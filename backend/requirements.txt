# Web 框架
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6

# 点云处理
numpy>=1.24.0
laspy>=2.3.0
open3d>=0.18.0
scipy>=1.11.0
# CSF 布料滤波（cloth simulation filter）— /api/filter-separate 端点依赖
# 注：部分旧版包名是 cloth-simulation-filter，新版可能合并到 csf，统一声明前者
cloth-simulation-filter>=1.0.0

# 深度学习（可选，RandLA-Net 推理）
# torch>=2.0.0
